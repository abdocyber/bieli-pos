name: bieli_pos
description: "نظام بيع لي لإدارة نقاط البيع السريعة والمخزون وطباعة الفواتير والنسخ الاحتياطي"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_fonts: ^6.2.1
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.2
  path: ^1.9.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  printing: ^5.13.1
  pdf: ^3.10.7
  url_launcher: ^6.2.5
  file_picker: ^8.0.0+1
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true