import 'package:flutter_test/flutter_test.dart';

import 'package:bieli_pos/main.dart';

void main() {
  test('application root can be constructed', () {
    expect(const BieLiApp(), isA<BieLiApp>());
  });
}
